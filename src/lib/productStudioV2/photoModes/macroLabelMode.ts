export function buildMacroLabelMode(): string {
  return [
    'INTERACTION_MODE: macro label detail.',
    'APPLICATION_ZONE: front label.',
    'CONTACT_SURFACE: none.',
    'PRODUCT_GROUNDING: true.',
    'LOCAL_DEFORMATION: none.',
  ].join(' ');
}
